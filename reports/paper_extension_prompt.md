# Prompt for the paper-extension agent (C-SPINE qualitative update)

Copy everything below the line into a fresh agent session AFTER you have replaced
`final_research (1).zip` in the repo root with the new Colab `final_research.zip`
produced by the fixed `research_full_run.ipynb`.

---

You are extending the IEEE conference paper `ieee_cspine_revisited.tex`
(C-SPINE Revisited) with the results of a NEW Colab run. The codebase has
already been fixed: qualitative analysis and all plots are now generated by
the notebook. Your job is paper text + figures + tables only. Do not retrain
models. Do not change code unless a path/figure reference is broken.

## 0. Inputs (read all of these first)

1. `ieee_cspine_revisited.tex` — the current paper (IEEEtran, ~473 lines).
   Read it end to end before editing.
2. The NEW `final_research.zip` in the repo root (it replaces the file
   previously named `final_research (1).zip`). Unzip to a scratch dir
   (e.g. `./zip_new/final_research/`) and inventory:
   - `metrics/sst2_test.json`, `metrics/agnews_test.json` (recon, sparsity,
     recon_vs_l0, downstream incl. dense/reconstructed/sparse/pca/randproj)
   - `metrics/sparsity_sweep.json`, `metrics/ablations.json`,
     `metrics/transfer.json`, `metrics/stability.json`, `metrics/status.json`,
     `metrics/hypotheses.json`
   - `metrics/interp_{sst2,agnews}.json` — feature blocks with keys:
     class, class_name, dim, mean_act, spec_score, purity (@50),
     purity_at_ks (@10/@20/@50), per_class_stats (mean/std/frac_nonzero),
     top_examples (in-class top-5), bottom_examples (in-class weakest-3),
     global_top (unconstrained top-5, mismatch check), coherence
     (dense-space mean pairwise cosine of top-20), description
   - `metrics/qual_summary_{sst2,agnews}.json`, `metrics/nn_{sst2,agnews}.json`
     (dense-vs-sparse top-5 NN Jaccard per query + mean)
   - `tables/qual_{sst2,agnews}_table.{txt,md,tex,csv}`, `tables/qual_summary.csv`,
     `tables/summary.csv`
   - `plots/training_*.png`, `plots/acthist_*.png`, `plots/heatmap_*.png`,
     `plots/recon_vs_l0_*.png`, `plots/downstream_*.png`,
     `plots/sparsity_tradeoff.png`, `plots/mechanisms.png`, `plots/transfer.png`
   - `plots/qual_{ds}_pop_hist.png` (purity/spec population),
     `plots/qual_{ds}_selectivity.png` (per-dim class bars),
     `plots/qual_{ds}_coactivation.png` (Pearson-r redundancy),
     `plots/qual_{ds}_coherence.png` (purity-vs-coherence bars)
   - `logs/*_training_log.csv`, `checkpoints_or_metadata/checkpoints.json`
3. `cspine/interpretability.py` + `cspine/visualization.py` docstrings
   (function semantics) and `reports/colab_fast_run_findings.md` (what the
   previous smoke run showed, incl. honest negatives).
4. `reports/audit_report.md` + `reports/final_report.md` (audit + evidence
   mapping H1–H6; do not contradict them without evidence).

## 1. What the fixed notebook added (so you know what to look for)

- Eval plots now wired correctly: acthist gets sparsity stats, heatmap gets
  spec stars + true dim ids, plus previously-missing `recon_vs_l0`,
  `downstream_bars` (accuracy + macro-F1 + per-class F1), `transfer_matrix`,
  `mechanism_bars`; sweep gets the dense-baseline line.
- Qualitative tests per showcased dim (3 dims/class × 5 top + 3 bottom +
  5 global): purity@K curve (10/20/50 — is purity K-robust?),
  per-class mean/std/firing-rate (strong-few vs weak-many?), dense-space
  coherence (do top activators sit near each other in BERT space?),
  dim co-activation r (duplicated atoms?), dense-vs-sparse NN Jaccard@5
  (did sparsification keep neighbourhoods?), example→dims dual view.
- Vocabulary discipline (mandatory in text): discriminative ≠ correlated ≠
  coherent ≠ interpretable. Purity = correlation evidence only.
- Paper-ready tables: `tables/qual_*_table.tex` (booktabs, `\\input`-able).

## 2. Edits to make in `ieee_cspine_revisited.tex`

1. **Abstract + Table I (main results):** replace smoke numbers ONLY with the
   new locked-TEST values (dense/recon/sparse acc + macro-F1, PCA, MSE/cos,
   L0/sparsity/dead). Keep the "consistency, not sparse>dense" framing when
   deltas are within ±2pp at n=500. Retain honest negatives (PCA lead,
   recon<sparse puzzle if it persists, TopK fidelity-vs-waste, MiniLM/H<D
   caveat if present).
2. **Sweep (H2):** update L0/MSE/acc per level + recon_vs_l0 reading
   (steep-then-flat? thresholds?). Reference `sparsity_tradeoff.png` with the
   dense line.
3. **Ablations:** update mechanism bars (VAL MSE / L0 / dead) + norm-stats
   sentence (raw CLS norm mean±std → relative σ=0.1 magnitude).
4. **Generalization/transfer:** update off-diagonal MSE/cosine/L0/dead +
   asymmetry direction; reference `transfer.png`.
5. **Interpretability (biggest change):** rewrite/extend with the NEW evidence:
   - population medians (purity@50, spec) so showcased dims are not
     cherry-picked (`qual_*_pop_hist.png`);
   - per-dim selectivity bars (`qual_*_selectivity.png`);
   - 2–4 named dims per dataset with purity@K curve, per-class firing rates,
     top-2 + bottom-1 + global-top-1 exemplars QUOTED from
     `tables/qual_*_table.txt` (truncate to ~110 chars, escape LaTeX);
   - coherence vs purity reading (`qual_*_coherence.png`) — call out any
     pure-yet-incoherent dim and any class-mismatch dim (selected for class A,
     majority-top from class B) as correlation≠interpretation;
   - redundancy (`qual_*_coactivation.png`, max off-diagonal r);
   - NN preservation mean Jaccard + 1–2 worked query examples.
   - `\\input` or inline-adapt `tables/qual_sst2_table.tex`
     (and agnews if space allows; else one in-paper + one in appendix/footnote).
   - Add 2–3 new `\\begin{figure}` blocks for the qual plots with
     self-contained captions (question → observation → implication).
6. **Limitations/threats:** update n/seed/H/epoch actually run; single-seed
   caveat if still 1 seed; probe-scaling asymmetry; ε-threshold note;
   purity=correlation caveat; CI-at-small-n caveat.
7. **Reproducibility:** confirm determinism only if re-runs match; list new
   artifacts (qual tables/plots/metrics) and their paths.
8. **Conclusion:** mirror the updated verdicts; no new claims beyond evidence.

## 3. Hard constraints

- IEEEtran must still compile: no new `\\usepackage` unless standard
  (booktabs/amsmath already implied); keep `\\label`/`\\ref` consistent;
  keep figures `width=\\columnwidth`; fix `\\graphicspath` if you move art.
- Every number in text/table must trace to a metrics/logs file — add a
  comment `% src: metrics/...` next to changed values if helpful.
- Small deltas (<2pp at n≈500, or overlapping CIs) are "consistent", never
  "wins". Negative/inconclusive results MUST be retained, not deleted.
- Do not equate class correlation (purity) with interpretability anywhere.
- Do not claim H4 (stability) unless ≥2 seeds with matching stats exist;
  otherwise state "deferred".
- Output: (a) updated `.tex`, (b) bullet changelog (old→new per number,
  figure/table added, section touched), (c) list of copied figure files with
  final paths, (d) compile check (`pdflatex`/`latexmk` log tail) or explicit
  statement that no TeX toolchain was available.
