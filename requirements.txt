# C-SPINE project dependencies
# Install with:  pip install -r requirements.txt
# NOTE (Colab): torch is preinstalled — the notebook does NOT upgrade it.

torch>=2.0
transformers>=4.35
datasets>=2.14
scikit-learn>=1.3
numpy>=1.24
tqdm>=4.66
matplotlib>=3.7
seaborn>=0.12
scipy>=1.10
